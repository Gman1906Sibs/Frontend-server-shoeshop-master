import express from "express";
import asyncHandler from "express-async-handler";

const payfastPayment = express.Router();



// GET ALL PRODUCT
paymentRoute.get(
  "/",
  asyncHandler(async (req, res) => {
    console.log(res)
  })
);

